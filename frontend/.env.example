# Copy to .env.local for local overrides.
#
# Leave unset to auto-resolve: localhost during normal development, and the
# page's own host when opened from another device on the LAN.
# VITE_API_URL=http://localhost:8000/api/v1
